import {Button} from "@mui/material";

const Login = () => {
    return (
        <div>
            <Button onClick={() => alert('Logged In')}>Login</Button>
        </div>
    )
}

export default Login